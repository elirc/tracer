# Copy to .env and adjust as needed.
# Postgres (see packages/db/docker-compose.yml for the matching local container)
DATABASE_URL="postgresql://tracer:tracer@localhost:5432/tracer?schema=public"

# API
API_PORT=3001

# Web (Vite dev server)
VITE_API_URL="http://localhost:3001"
VITE_WS_URL="ws://localhost:3001/ws"
